Use `uv` to create and manage the environment for this repository.

Example:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv sync
```

OpenSim itself is expected to be installed separately with Python bindings available to the interpreter used by `uv run`.
